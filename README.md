# MN Portfolio

Upload everything in this folder to the root of your GitHub repository, then enable GitHub Pages (Settings → Pages → Deploy from branch → main → / root).

Keep the files together: index.html needs support.js, image-slot.js, image-slots.state.json and the images folder.
